/**
 * Central icon module. HANDOFF.md §2 says "No emoji in product chrome" — every
 * glyph in the UI comes from lucide-react so the stroke weight stays consistent.
 */
export {
    Home,
    Calendar,
    Upload,
    Trophy,
    Shield,
    Bell,
    Check,
    X,
    ArrowRight,
    ImageIcon,
    MapPin,
    Award,
    FileText,
    Megaphone,
} from 'lucide-react';
